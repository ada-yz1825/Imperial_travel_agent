# Imperial Travel Agent 面试问答整理

## 1. 先用一句话介绍一下这个项目

**答：**  
这是一个面向 Imperial College London 学生和访客的智能出行与学习空间助手。它把大模型的自然语言问答能力，和 Google Routes、Google Weather、TfL 实时状态、以及本地维护的校园学习空间数据结合起来，让用户可以直接提问路线、天气、延误情况和学习场地推荐。

## 2. 这个项目主要解决什么问题？

**答：**  
我想解决的是“信息分散”的问题。用户在真实场景里，往往不是只想查路线，还会同时关心目的地天气、地铁是否延误、附近有没有适合学习的地方。这个项目把这些需求放进一个统一入口里，让用户用自然语言一次性获取更完整的决策支持。

## 3. 这个项目的核心功能有哪些？

**答：**  
核心功能主要有五类：

1. 自然语言路线规划，支持从当前选点或指定地点出发。
2. 天气查询，支持起点、当前位置和导航目的地。
3. TfL 线路实时状态查询，用来判断路线是否受延误影响。
4. 学习与工作空间推荐，根据偏好和距离做排序。
5. 路线可视化，把推荐路线绘制成可交互地图。

## 4. 这个项目的整体架构是怎样的？

**答：**  
我把它拆成四层：

1. 前端层：`app.js` + `index.html` + `styles.css`，负责页面交互、地图展示、流式回答渲染和双语切换。
2. HTTP 适配层：`server.py`，负责给浏览器提供稳定的 REST API。
3. MCP 工具层：`mcp_server.py`，负责注册工具、处理工具调用、编排多步 Agent 流程。
4. 核心能力层：`navigator_core.py`，负责地点解析、路线请求、天气请求、模型调用、回答清洗和格式化。

这样的分层能让前端、接口适配和智能体逻辑解耦。

## 5. 为什么用了 MCP 思路？

**答：**  
因为这个项目不是单纯聊天，而是“模型 + 外部工具”的场景。MCP 这种模式很适合把 `navigate`、`weather_current`、`tfl_status`、`web_search` 这些能力标准化，让模型在需要时调用工具，而不是直接凭空生成答案。这样可以明显降低幻觉，提高可维护性。

## 6. Agent 在这个项目里是怎么工作的？

**答：**  
流程大概是：

1. 前端把用户问题、起点上下文、历史消息发给后端。
2. `server.py` 把请求转给 `mcp_server.py` 的 `agent_answer`。
3. `agent_answer` 给模型一套 system prompt 和可用工具 schema。
4. 模型自己判断要不要调用工具，以及调用哪个工具。
5. 工具返回结果后，再把结果喂回模型生成最终答案。
6. 如果是路线请求，还会附带 route map 数据，让前端把地图嵌到回答里。

所以这里不是“模型直接回答”，而是“模型先决策，再调用工具，再整合结果”。

## 7. 这个项目里有哪些主要工具？

**答：**  
主要工具有：

1. `navigate`：调用 Google Routes 获取路线、时长、距离和线路信息。
2. `render_route_map`：把路线结果转成前端可渲染的地图数据。
3. `weather_current`：获取当前天气。
4. `tfl_status`：查询 TfL 实时线路状态。
5. `route_matrix`：做一个起点到多个候选地点的时间比较。
6. `web_search`：处理百科或公开事实类问题。

## 8. 为什么要把 `navigate` 和 `render_route_map` 分开？

**答：**  
这是一个比较重要的设计点。  
`navigate` 的职责是获取事实型路线数据，比如 duration、distance、transit lines、stops；`render_route_map` 的职责是把这些数据转成 UI 需要的地图展示内容。分开以后，路线计算和前端展示解耦，后面如果地图展示方式要改，不需要影响路线工具本身。

## 9. 路线数据来自哪里？

**答：**  
主要来自 Google Routes API。  
它提供实时路线、出行方式对比、距离、预计时长，以及路线几何信息。项目里会把这些结果标准化后交给前端和模型使用。

## 10. TfL 状态为什么单独做，而不是只看 Google 路线？

**答：**  
因为 Google Routes 更偏路线规划，而 TfL Unified API 更适合做伦敦本地线路运营状态的补充。  
用户问“这条路线会不会受延误影响”时，只给路线不够，还要看当前线路是否 disrupted。所以项目里会先拿到路线涉及的 transit lines，再进一步调用 `tfl_status` 做补充判断。

## 11. 这个项目是怎么减少模型幻觉的？

**答：**  
我主要做了几层约束：

1. 能用工具的场景优先用工具，不让模型猜实时数据。
2. prompt 里明确要求只能引用工具返回的线路名、站点名和状态。
3. 导航回答生成后，还会做清洗和校验，比如去掉错误的起终点开头、去掉推理痕迹。
4. 如果工具没有返回特定线路信息，就明确告诉用户“工具没有提供”，而不是编造。

本质上是让模型负责“组织语言”，而不是负责“虚构事实”。

## 12. 这个项目为什么支持多模型？

**答：**  
因为我希望它既能本地跑，也能接不同云端模型，所以在 `navigator_core.py` 里抽象了 provider 层，支持 OpenAI、Groq、Together 和 Ollama。  
这样有几个好处：

1. 便于对比不同模型的效果和成本。
2. 云端服务异常时可以切换。
3. 本地开发时可以先用 Ollama 做基础联调。

## 13. 多模型切换在前端是怎么体现的？

**答：**  
前端有一个 LLM 信号卡片和模型选择弹层。用户可以切换当前对话模型，前端会把 `chatModelId` 带到请求里，后端再根据配置把它解析到具体模型。这个设计让模型切换对用户可见，也方便演示“同一套 Agent 逻辑，不同模型都能跑”。

## 14. 学习空间推荐是怎么做的？

**答：**  
这部分是“本地数据 + 启发式评分”。  
前端维护了一组学习空间元数据，比如位置、标签、comfort、是否适合 group work、是否安静等。然后结合用户选择的场景，比如 quiet、group、late、nearest，以及步行容忍度，计算一个综合分数并排序展示。

## 15. 为什么学习空间推荐没有直接交给大模型做？

**答：**  
因为这类排序逻辑其实更适合规则和结构化评分，而不是完全交给模型。  
如果完全由模型决定，结果不稳定，也不容易解释。现在的做法是先用确定性的规则把候选结果排好序，再在需要的时候让模型做自然语言解释，这样更稳。

## 16. 这个项目是怎么处理中英文双语的？

**答：**  
前端有完整的 i18n 文案映射，界面文案在 `app.js` 里统一管理。  
后端和模型侧则会根据用户输入判断回答语言，如果检测到中文，就要求模型用中文回答。这样 UI 和回答都能保持同一语言，不会出现界面中文但回答英文的割裂感。

## 17. 为什么要自己做流式回答渲染？

**答：**  
因为这个场景里等待感比较强，尤其是路线、天气、TfL 这类多工具调用串起来时。  
流式渲染可以让用户更早看到回答在生成，也能配合前端的工具状态提示，让用户知道系统正在做什么，整体体验会比“长时间空白后一次性返回”好很多。

## 18. 前端除了聊天，还做了哪些复杂交互？

**答：**  
前端其实承担了很多工作：

1. Google Map 选点和拖动标记。
2. 路线地图绘制，包括 polyline、站点 marker、全屏模式。
3. 天气卡片渲染和 AI 简报展示。
4. TfL 状态卡片和 tooltip。
5. 流式回答、Markdown 渲染、聊天历史展示。
6. 双语切换和模型切换。

所以它不只是一个“输入框 + 输出框”。

## 19. 路线地图是怎么画出来的？

**答：**  
后端会把路线几何信息和站点信息整理好，前端基于 Google Maps JavaScript API 去画 polyline、起终点 marker，以及中间站点 marker。  
如果是公共交通，还会尽量按线路颜色去渲染不同 segment，这样用户能更直观看到换乘结构。

## 20. 为什么后端还保留了 HTTP API，而不是让前端直接连 MCP？

**答：**  
因为浏览器直接对接 MCP 不够稳定，也不适合直接暴露底层工具细节。  
`server.py` 的作用就是做一个薄适配层，对前端提供固定接口，比如 `/api/chat`、`/api/navigate`、`/api/routes`、`/api/intent`。这样前端更简单，MCP 层也可以独立演进。

## 21. `server.py` 的作用除了转发还有什么？

**答：**  
除了转发，它还做了几件事：

1. 管理 stdio 模式下的 MCP 子进程连接。
2. 支持用 HTTP 连远程 MCP 服务。
3. 对聊天结果做流式输出。
4. 暴露健康检查接口。
5. 单独提供 TfL 状态查询和地理编码接口。

所以它既是 adapter，也是一个轻量的服务编排层。

## 22. 项目里为什么要保留 `navigator_core.py` 这种共享核心模块？

**答：**  
这是为了避免逻辑散落。  
比如环境变量解析、provider 选择、地点解析、Google API 调用、回答后处理、天气摘要 prompt，这些能力如果都写在 `mcp_server.py` 或 `server.py` 里会很乱。抽到 `navigator_core.py` 后，整个项目会更清晰，也方便复用。

## 23. 这个项目在地点解析上做了什么处理？

**答：**  
它先维护了一套 `KNOWN_LOCATIONS`，覆盖常见校区和图书馆别名，包括中英文。  
如果命中已知地点，就直接返回结构化坐标；如果没有命中，再回退到 Google Geocoding。这样既提高了常见校园场景的准确率，也保留了对开放地点输入的支持。

## 24. 如果用户只说“带我去 White City”，系统怎么知道起点？

**答：**  
项目会把前端当前选中的起点作为 `contextStart` 一起传给后端。  
所以当用户只说目的地时，Agent 可以把页面上的选点上下文当作默认起点使用。这一点很关键，因为真实用户不会每次都把完整起终点写出来。

## 25. 这个项目有哪些工程上比较好的地方？

**答：**  
我觉得有几个点比较值得讲：

1. 分层清晰，前端、HTTP 适配、MCP 工具、核心逻辑解耦。
2. 工具职责明确，路线、天气、TfL、搜索各自独立。
3. 多模型 provider 抽象比较完整。
4. 对模型输出做了清洗和约束，而不是完全信任模型。
5. 用户体验比较完整，不只是 API demo，而是一个可交互产品原型。

## 26. 这个项目最大的技术难点是什么？

**答：**  
我觉得最大的难点不是单个 API 调通，而是“怎么把多源实时数据和模型回答整合成一个自然、可信、可视化的用户体验”。  
难点主要体现在三点：

1. 什么时候调用哪个工具。
2. 怎么避免模型把工具结果说错。
3. 怎么让路线、地图、天气和 TfL 状态在一个回答里自然衔接。

## 27. 如果面试官问“你做了哪些防御性设计”，可以怎么答？

**答：**  
可以从这几个角度说：

1. 没有 key 时，项目会退化，而不是直接崩溃。
2. 模型调用失败时，有 fallback 逻辑。
3. 路线没有返回结果时，会明确提示用户改写地点或去 Google Maps 确认。
4. 对天气和地理数据覆盖不佳的区域，比如中国大陆，会给出提示。
5. 对工具返回值做了标准化和过滤，减少异常数据影响前端。

## 28. 如果面试官问“你怎么做性能和体验优化”，可以怎么答？

**答：**  
我会说这几个点：

1. 聊天回答用了流式输出，降低用户等待焦虑。
2. 前端把路线图嵌入回答区，而不是跳新页面，减少交互中断。
3. 学习空间推荐先本地算分，避免每次都走模型。
4. 模型调用上限制了历史长度和 token 数，避免无谓开销。
5. 工具只在必要时触发，不会所有请求都全量调用。

## 29. 这个项目有哪些局限？

**答：**  
我会比较诚实地说几个局限：

1. 学习空间数据是本地静态数据，不是实时占座信息。
2. 路线和天气质量依赖外部 API 覆盖范围。
3. 如果模型 provider 不稳定，回答体验会受影响。
4. 当前更适合伦敦和 Imperial 场景，泛化到别的城市还需要补数据和规则。

这类回答一般不会减分，反而体现你对项目边界有判断。

## 30. 如果继续迭代，你最想做什么？

**答：**  
我会优先做三件事：

1. 引入实时 occupancy 或开放时间数据，让学习空间推荐更真实。
2. 增加用户偏好记忆，比如偏好安静、自习、少走路。
3. 把路线解释和延误风险评估做得更细，比如给出换乘风险和备选路线策略。

## 31. 如果面试官问“你在这个项目里最能体现你能力的部分是什么”，怎么答？

**答：**  
我会说最能体现我能力的，不是单纯接了几个 API，而是把一个“多工具、多数据源、多模态交互”的系统整合成了一个可用产品原型。  
这里面既有工程分层，也有 Agent 工具编排，也有前端体验设计，还包含对模型不确定性的控制。

## 32. 如果让你用 1 分钟完整介绍项目，可以怎么说？

**答：**  
这个项目叫 Imperial Travel Agent，是一个面向 Imperial College London 学生和访客的智能出行与学习空间助手。用户可以直接用自然语言提问，比如怎么去某个校区、目的地天气怎么样、伦敦地铁有没有延误、附近哪里适合学习。系统前端负责地图选点、路线展示和双语交互，后端通过一个 HTTP adapter 连接 MCP 工具层，再由 Agent 根据问题选择调用 Google Routes、Google Weather、TfL 和 web search 等工具。为了降低大模型幻觉，我把实时数据尽量交给工具获取，再让模型只负责整合和表达。整体上它不是一个纯聊天机器人，而是一个“模型 + 外部工具 + 可视化界面”的完整产品原型。

---

## 高频追问速答

### Q: 你这个项目更偏前端还是后端？

**答：**  
我觉得是全栈偏后端智能体方向。前端交互很完整，但项目最核心的难点在于工具编排、数据整合和回答可信度控制。

### Q: 你为什么不直接让前端请求 Google API？

**答：**  
因为很多逻辑需要后端统一编排，尤其是 Agent 工具调用、多模型切换、结果清洗和多数据源整合。直接放在前端会让逻辑分散，也不利于后续扩展。

### Q: 这个项目里最像 Agent 的地方是什么？

**答：**  
最像 Agent 的地方是模型不是直接回答，而是先判断问题类型，再自主决定是否调用工具、调用哪个工具、是否串联多个工具，最后再生成答案。

### Q: 你怎么证明它不是普通 prompt engineering？

**答：**  
因为这里有明确的工具 schema、工具调用循环、工具结果回填、状态跟踪、路由后再触发 TfL 查询、以及 route map 的结构化内容注入，这已经不是单轮 prompt 了，而是一个有执行链路的 Agent 系统。

### Q: 你做这个项目学到了什么？

**答：**  
我最大的收获是，做 Agent 应用真正难的不是“让模型能回答”，而是“让模型在该闭嘴的时候闭嘴，在该调用工具的时候调用工具，并把结果稳稳地落到产品体验里”。

---

## 复习建议

面试前优先背这四条主线：

1. 项目目标：把路线、天气、TfL 和学习空间整合进一个自然语言助手。
2. 架构分层：前端 -> HTTP adapter -> MCP tool layer -> core logic。
3. 关键设计：工具调用替代模型猜测，降低幻觉。
4. 个人贡献表达：不只是接 API，而是把多源实时数据和 Agent 交互做成了一个完整产品原型。
