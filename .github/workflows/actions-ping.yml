name: Actions Ping

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Print runner status
        run: echo "GitHub Actions runner is available."
